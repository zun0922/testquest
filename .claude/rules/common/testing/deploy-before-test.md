---
paths:
  - "tests/**"
  - ".github/workflows/**"
---

# デプロイ環境テスト前の反映確認（必須）

- コード修正後にデプロイ環境（本番・プレビュー）でテストする場合、`git commit` + `git push` が完了してからテストを開始する
- push から自動デプロイ完了まで時間差がある（Vercel 等で約 30〜60 秒）。**デプロイ完了前にテストすると FAIL が不正に記録される**
- デプロイ状況はホスティングのダッシュボードまたはデプロイ済みコミットハッシュで確認する
