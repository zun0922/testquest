# テスト手順書 v0.1 — §5 システムテスト手順（分割パート）

> 本ファイルはテスト手順書 v0.1 の §5（システムテスト手順）。基盤（§1〜§3）は本体 `テスト手順書_v0.1.md` を参照。形式・確認の期待値は本体 §1 に従う。全機能実装後に実施する。

---

## 5.1 シナリオテスト（ST-SCN）

### ST-SCN-001-TC-001
- **観点ID**：ST-SCN-001 ／ **対応要件**：AC-003〜007 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. セーブ無し状態でタイトル画面を表示していること

**手順**
1. 「はじめから」→章選択→任意のシナリオを開始する
2. 選択肢のあるシーンで3〜5回選択を行いながら最後まで進める
3. 結果画面まで到達し「章選択へ戻る」で章選択に戻る

**確認**
1. 全工程 → 期待値：各画面が仕様どおり遷移し、破綻なく完結する
2. クリア記録 → 期待値：§1.3 で cleared に当該シナリオが記録される

**後処理**
なし

**合否判定**
- PASS：確認1・2が両方成立／FAIL：いずれか不成立

### ST-SCN-001-TC-002
- **観点ID**：ST-SCN-001 ／ **対応要件**：AC-008 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. プレイ中のシナリオがあること

**手順**
1. プレイ中に中断する
2. タイトルで「つづきから」を選ぶ

**確認**
1. 復元 → 期待値：クリア済み状況・ステータスが保持され再開できる

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

### ST-SCN-001-TC-003
- **観点ID**：ST-SCN-001 ／ **対応要件**：AC-009 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. クリア済みシナリオがあること。§1.3 で status を控えておくこと

**手順**
1. クリア済みシナリオを再プレイし結果画面まで到達する
2. §1.3 で status を確認する

**確認**
1. 再プレイ可否 → 期待値：再プレイできる
2. status → 期待値：再プレイ前後で不変（FT-006-003 と整合）

**後処理**
なし

**合否判定**
- PASS：確認1・2が両方成立／FAIL：いずれか不成立

### ST-SCN-001-TC-004
- **観点ID**：ST-SCN-001 ／ **対応要件**：FR-002 ／ **テストデータ**：なし（本番FL第1章5本）／ **担当**：人間

**事前条件**
1. 第1章5本の実シナリオが揃っていること（コンテンツ依存）

**手順**
1. 第1章5本を順次クリアする

**確認**
1. 記録 → 期待値：5本すべてが cleared に記録される（§1.3）

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

### ST-SCN-001-TC-005
- **観点ID**：ST-SCN-001 ／ **対応要件**：AC-005 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. 分岐のあるシナリオを開始できること。TC-001（王道）で通ったノード列を把握していること

**手順**
1. 王道（best中心）以外の選択を行う（途中で別分岐へ進む・要改善（poor）を含む選択をする）
2. 王道と異なるルートで最後まで進める

**確認**
1. 到達 → 期待値：王道と異なるノード列を通り、行き止まり・未定義遷移なく結果画面まで到達する（分岐先の正しさは FT-004-002・全ルート到達性は CQ-fl-1-{nn}-TC-006／検証#11 が担保。本TCは別ルートの通し体感）

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

---

## 5.2 遷移テスト（ST-TR）

### ST-TR-001-TC-001
- **観点ID**：ST-TR-001 ／ **対応要件**：要件10章 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. アプリを起動していること

**手順**
1. タイトル↔章選択↔再生↔結果の各遷移トリガーを順に操作する

**確認**
1. 遷移 → 期待値：状態遷移図どおりに全遷移（タイトル↔選択↔再生↔結果）が一致する

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

### ST-TR-001-TC-002
- **観点ID**：ST-TR-001 ／ **対応要件**：FR-003 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. シナリオ再生中であること

**手順**
1. 再生中にブラウザをリロードする

**確認**
1. 表示 → 期待値：タイトル画面から再開する（シナリオ途中状態は失われる・FR-003）

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

### ST-TR-001-TC-003
- **観点ID**：ST-TR-001 ／ **対応要件**：要件10章 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. 各画面を表示できること

**手順**
1. 各画面で想定外の操作（連打・戻る・想定しないボタン順）を試す

**確認**
1. 遷移 → 期待値：状態遷移図にない未定義の遷移が発生しない

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

---

## 5.3 パフォーマンステスト（ST-PF）

> 計測は Lighthouse CLI ＋ ブラウザ Performance を用い、複数回計測して最大値を記録する（テスト仕様書§3.3）。手動目視では下記の視覚トリガーでストップウォッチ計測し、精度が必要な場合は Lighthouse/Playwright 計測を併用する。

### ST-PF-001-TC-001
- **観点ID**：ST-PF-001 ／ **対応要件**：要件7.1 ／ **テストデータ**：なし ／ **担当**：人間 / AI

**事前条件**
1. キャッシュなし（Ctrl+Shift+R 後）の状態であること

**計測区間の定義**
- 開始：アドレスバーで URL を確定（Enter 押下）した瞬間
- 終了：タイトル画面のタイトル文字・ボタンが描画され操作可能になった瞬間

**手順**
1. URL を確定してタイトル画面を表示する（複数回計測）
2. Lighthouse CLI でも初期表示系指標を計測する

**確認**
1. 初期表示時間 → 期待値：複数回計測の最大が **3秒以内**

**後処理**
なし

**合否判定**
- PASS：最大が3秒以内／FAIL：3秒を超える

### ST-PF-001-TC-002
- **観点ID**：ST-PF-001 ／ **対応要件**：要件7.1 ／ **テストデータ**：なし ／ **担当**：AI（Playwright計測）主・人間目視は参考

> 0.5秒は手動ストップウォッチでは反応誤差が許容を超えるため、**Playwright での自動計測を主**とする（人手目視は参考値）。

**事前条件**
1. シナリオ再生中であること

**計測区間の定義**
- 開始：メッセージウィンドウへの `page.click()` 実行時（手動の場合：クリックした瞬間）
- 終了：次シーンの本文要素が `page.waitForSelector(...)` で検出された時（手動の場合：本文・立ち絵が描画された瞬間）

**手順**
1. Playwright で `page.click()`（シーン送り）→ 次シーン本文の `waitForSelector` までの経過時間を計測する（複数回）
2. 参考として人間も目視でストップウォッチ計測する（精度は参考扱い）

**確認**
1. シーン遷移時間（Playwright計測値） → 期待値：複数回計測の最大が **0.5秒以内**

**後処理**
なし

**合否判定**
- PASS：最大が0.5秒以内／FAIL：0.5秒を超える

### ST-PF-001-TC-003
- **観点ID**：ST-PF-001 ／ **対応要件**：要件7.1 ／ **テストデータ**：なし ／ **担当**：人間 / AI

**事前条件**
1. 章選択画面を表示していること

**計測区間の定義**
- 開始：シナリオカードをクリックした瞬間
- 終了：再生画面（`screen-play`）の最初のシーンが描画された瞬間

**手順**
1. シナリオ選択から再生開始までを計測する（複数回）

**確認**
1. JSONロード時間 → 期待値：複数回計測の最大が **2秒以内**

**後処理**
なし

**合否判定**
- PASS：最大が2秒以内／FAIL：2秒を超える

---

## 5.4 構成テスト（ST-CF）

> Chrome・Edge・Safari × PC・スマホ横持ち（844×390 目安）で確認（テスト仕様書§3.2）。Windows環境では Safari 部分はスキップとし、テストログ `notes` に理由を記録する。

### ST-CF-001-TC-001
- **観点ID**：ST-CF-001 ／ **対応要件**：要件7.2 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. Chrome / Edge / Safari × PC / スマホ横持ち の各組み合わせで表示できること

**手順**
1. 各ブラウザ×各画面サイズでプレイ画面・選択画面を表示する（重点確認）

**確認**
1. レイアウト → 期待値：本文200字が収容され、レイアウト崩れがない（全ブラウザ）

**後処理**
なし

**合否判定**
- PASS：全対象でレイアウト崩れなし／FAIL：いずれかで崩れる

### ST-CF-001-TC-002
- **観点ID**：ST-CF-001 ／ **対応要件**：FR-008 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. 各ブラウザで起動できること

**手順**
1. 各ブラウザでクリア→リロード→つづきからを実施する（重点確認）

**確認**
1. 保存・復元 → 期待値：全ブラウザで保存・復元が動作する

**後処理**
なし

**合否判定**
- PASS：全ブラウザで動作／FAIL：いずれかで不動作

### ST-CF-001-TC-003
- **観点ID**：ST-CF-001 ／ **対応要件**：FR-003 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. 各ブラウザで再生画面を表示できること

**手順**
1. 各ブラウザでテキスト送り・全文表示を操作する（重点確認）

**確認**
1. テキスト送り → 期待値：送り・全文表示が全ブラウザで動作する

**後処理**
なし

**合否判定**
- PASS：全ブラウザで動作／FAIL：いずれかで不動作

### ST-CF-001-TC-004
- **観点ID**：ST-CF-001 ／ **対応要件**：要件7.2 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. 主ブラウザ（Chrome）で各画面を表示できること

**手順**
1. ボタン操作・画面遷移・グレーアウトを目視確認する

**確認**
1. 表示・操作 → 期待値：重大な崩れ・不動作がない（目視）

**後処理**
なし

**合否判定**
- PASS：重大な崩れ・不動作なし／FAIL：あり

---

## 5.5 ユーザビリティテスト（ST-UB）

### ST-UB-001-TC-001
- **観点ID**：ST-UB-001 ／ **対応要件**：AC-011 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. スマートフォン実機（横持ち・844×390 目安）でアプリを開けること

**手順**
1. スマホを**横持ち**にして はじめから→章選択→シナリオ→選択→結果 まで通しでプレイする
1. 続けて端末を**縦持ち**にする

**確認**
1. 完結 → 期待値：全フローがスマホ横持ちで完結する（AC-011）
1. 縦持ち → 期待値：回転案内「端末を横向きにしてください」が全画面表示され、背後の操作ができない（AC-011・設計書§8.4）

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

### ST-UB-001-TC-002
- **観点ID**：ST-UB-001 ／ **対応要件**：要件7.2 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. スマホ実機で各ボタンを表示していること

**手順**
1. 各ボタンのタップ領域を確認する（DevTools のデバイスモードまたは実機で寸法確認）

**確認**
1. タップ領域 → 期待値：最小 44×44px

**後処理**
なし

**合否判定**
- PASS：全ボタンが44×44px以上／FAIL：いずれかが下回る

### ST-UB-001-TC-003
- **観点ID**：ST-UB-001 ／ **対応要件**：UI-RULE-005 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. アプリを操作できること

**手順**
1. 全操作をクリック／タップのみで行えるか確認する

**確認**
1. 操作 → 期待値：キーボード入力なしで全操作が完結する（UI-RULE-005）

**後処理**
なし

**合否判定**
- PASS：確認1が成立／FAIL：不成立

---

## 5.6 回復性テスト（ST-RC）

> FT-009-002・FT-008-002 と一部重複するが、ST-RC はシステム視点（実ネットワーク遮断・実ブラウザのプライベートモード）で再確認する。FT が PASS 済みであることを前提に実施する（テスト仕様書§5.6）。

### ST-RC-001-TC-001
- **観点ID**：ST-RC-001 ／ **対応要件**：FR-009 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. アプリを起動していること。DevTools の Network で回線を操作できること

**手順**
1. DevTools の Network を「Offline」にする
2. シナリオロードを発生させ、エラー画面を表示させる
3. Network を「Online」に戻し、エラー画面の「再読み込み」をクリックする

**確認**
1. 回復 → 期待値：エラー画面→再読み込みで正常化する（シナリオが表示される）

**後処理**
1. Network を Online に戻す

**合否判定**
- PASS：確認1が成立／FAIL：不成立

### ST-RC-001-TC-002
- **観点ID**：ST-RC-001 ／ **対応要件**：FR-008 ／ **テストデータ**：TD-STORAGE ／ **担当**：人間

**事前条件**
1. 実ブラウザのプライベートブラウズ（TD-STORAGE）でアプリを開いていること

**手順**
1. タイトル画面の注記を確認し、プレイできるか試す

**確認**
1. 表示・プレイ → 期待値：保存不可の注記が表示され、プレイは可能（FT-008-002-TC-001 と整合）

**後処理**
1. 通常ブラウズに戻す。テストログ `notes` に「TD-STORAGE使用・復元完了」を記録

**合否判定**
- PASS：確認1が成立／FAIL：不成立

---

## 5.7 セキュリティテスト（ST-SEC）

### ST-SEC-001-TC-001
- **観点ID**：ST-SEC-001 ／ **対応要件**：要件7.3 ／ **テストデータ**：TD-SEC（`<script>` 等を含むJSON）／ **担当**：人間 / AI

**事前条件**
1. AI が TD-SEC を `public/data/scenarios_test/sec/` に準備済みであること
2. 本体§1.6 の切替で `VITE_SCENARIOS_PATH=/data/scenarios_test/sec npm run dev` を指定して起動済みであること

**手順**
1. TD-SEC のシナリオを再生し、本文・解説に注入した `<script>alert(1)</script>` 等の箇所を表示する

**確認**
1. 表示 → 期待値：スクリプトが実行されず（アラートが出ない）、文字列としてエスケープ表示される

**後処理**
1. 環境変数なしで再起動し本番データに復帰。テストログ `notes` に「TD-SEC使用・復元完了」を記録

**合否判定**
- PASS：スクリプト非実行かつ文字列表示／FAIL：スクリプトが実行される

### ST-SEC-001-TC-002
- **観点ID**：ST-SEC-001 ／ **対応要件**：要件7.3 ／ **テストデータ**：なし ／ **担当**：AI（コマンド実行）

**事前条件**
1. 依存パッケージがインストール済みであること

**手順**
1. ターミナルで `npm audit` を実行する

**確認**
1. 出力 → 期待値：高・中リスクの脆弱性が0件

**後処理**
なし

**合否判定**
- PASS：高・中リスク0件／FAIL：高または中リスクが1件以上

---

## 5.8 コンテンツ品質テスト（CQ・Layer C）

### CQ-fl-1-01〜05-TC-001
- **観点ID**：CQ-fl-1-01〜05 ／ **対応要件**：AC-012 ／ **テストデータ**：各シナリオJSON ／ **担当**：監修者（JSTQB有資格者・ドキュメントレビュー）

**事前条件**
1. FL第1章5本のシナリオJSONと実機表示が用意されていること
2. 参照基準（JSTQB FL シラバス V4.0・ISTQB用語集）を参照できること

**手順**
1. 各シナリオJSONと表示テキストを監修者がレビューする（アプリ操作ではなく内容確認）

**確認**
1. 5観点 → 期待値：(1)評価の妥当性 (2)解説の正誤 (3)項番の対応 (4)用語準拠 (5)転載なし、を全て満たし承認される（AC-012）

**後処理**
1. 監修コメント・承認記録を残す（テスト計画書§8.1）

**合否判定**
- PASS：5観点を満たし監修承認／FAIL：いずれかの観点で不可

### CQ-fl-1-{nn}-TC-006
- **観点ID**：CQ-fl-1-{nn} ／ **対応要件**：FR-009（検証#11）／ **テストデータ**：各シナリオJSON ／ **担当**：AI（コマンド実行）/ 監修者

**事前条件**
1. validator の到達性検証（検証#11）が実装済みであること

**手順**
1. 各シナリオJSONを validator に通す（`npm run test -- validator` または全シナリオ通しテスト）

**確認**
1. 到達性 → 期待値：全ノードが到達可能・全ルートが最終ノードに到達（検証#11 の警告ゼロ・行き止まりなし）

**後処理**
なし

**合否判定**
- PASS：警告ゼロ・行き止まりなし／FAIL：到達不能ノードまたは行き止まりがある

---

*本パート（§5 システムテスト手順）で機能テスト（FR-001〜009）＋システムテストの手順を一通り収録した。テスト仕様書 v1.1 の全TCに対応する。*

## 5.9 Phase 2 の手動確認（ST-M2・v1.3 新設）

Phase 2 の5機能は E2E 28件で自動検証済み（テスト仕様書 §8.3）。本節は**機械に判定させられない観点だけ**を人間が見る。
いずれも「動くか」ではなく「**受け入れられるか**」を確かめる。

### ST-M2-001-TC-001
- **観点ID**：ST-M2-001 ／ **対応要件**：AC-013 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. iPhone 実機で本番 https://testsim-three.vercel.app を開き、**横持ち**にしていること
2. 端末のサイレントスイッチ・音量の状態を記録しておくこと（結果の解釈に必要）

**手順**
1. 「はじめから」→ FL第1章の任意のシナリオを開始する
2. 最初のセリフで**画面を1回タップ**する（自動再生ポリシーの解錠）
3. そのまま3〜5ノード進める

**確認**
1. 音声 → 期待値：セリフが聞こえる
2. 鳴らなかった場合 → 期待値：**進行は止まらない**（無音のまま最後まで操作できる）
3. サイレントスイッチON時の挙動 → 期待値：鳴らないが操作は継続できる

**後処理**
なし

**合否判定**
- PASS：確認2が成立（音が鳴るかどうかに関わらず**プレイを妨げない**）／FAIL：無音時に操作不能・画面停止が起きる
- ※ 音が鳴らない事象は端末設定の記録とセットで報告する（不具合とは限らないため）

### ST-M2-001-TC-002
- **観点ID**：ST-M2-001 ／ **対応要件**：AC-013 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. ST-M2-001-TC-001 で音声が鳴ることを確認済みであること

**手順**
1. セリフの再生中に画面をタップしてテキストを全文表示する
2. もう一度タップして次のノードへ進む

**確認**
1. 1回目のタップ → 期待値：テキストは全文表示されるが**音声は途切れない**（PO決定 2026-08-25）
2. 2回目のタップ → 期待値：前のセリフが止まり、次のセリフが鳴り始める

**後処理**
なし

**合否判定**
- PASS：確認1・2が両方成立／FAIL：1回目のタップで音声が切れる

### ST-M2-002-TC-001
- **観点ID**：ST-M2-002 ／ **対応要件**：AC-013 ／ **テストデータ**：なし ／ **担当**：人間

**手順**
1. 任意の章を音声ONで通しプレイし、専門用語の読み上げを聴く

**確認**
1. 読み → 期待値：JSTQB用語が正しく読まれる（例：同値分割・境界値分析・シラバス）

**後処理**
なし

**合否判定**
- PASS：意味が取れないほどの読み間違いが無い／FAIL：用語が別語に聞こえる
- ※ 読み間違いは `scripts/voice-dict.json` に登録して**該当ノードだけ**鳴らし直せる（費用ゼロ）。TC単位で差し戻さず、記録して次バッチでまとめて直す

### ST-M2-002-TC-002
- **観点ID**：ST-M2-002 ／ **対応要件**：AC-017 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. FL編を全クリア済み（AL編が解放されていること）

**手順**
1. シナリオ選択画面の AL-TTA の節で編成「伊藤 澪」を選ぶ
2. al-tta-1-01 を含む2〜3本を音声ONでプレイする

**確認**
1. 声と口調 → 期待値：本文は匠と共通（常体）のままだが、女性音源で聞いて不自然でない
2. 名乗り → 期待値：「伊藤だ」と名乗り、「高橋」が出てこない
3. 立ち絵 → 期待値：澪の立ち絵が表示され、匠が混在しない

**後処理**
1. 編成を「高橋 匠」へ戻す（既定に復す）

**合否判定**
- PASS：確認1〜3がすべて成立／FAIL：口調の違和感が学習の妨げになる、または名前・立ち絵の混在がある
- ※ 口調は `public/data/casting.json` の差分（現在7か所）で調整でき、**シナリオ本体は変更不要**

### ST-M2-003-TC-001
- **観点ID**：ST-M2-003 ／ **対応要件**：AC-014 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. ヒントが使える程度にポイントが貯まっていること（FL は合計60以上でLv1）

**手順**
1. FL・AL からそれぞれ3問ほど選び、「ヒントを見る」を押す

**確認**
1. ヒント文 → 期待値：**答えそのものではなく、考え方の道筋**を示している
2. 抽象度 → 期待値：読んでも何も分からない、という水準ではない

**後処理**
なし

**合否判定**
- PASS：両方成立／FAIL：ヒントが答えを言っている、または役に立たない
- ※ ヒントは `hint` フィールドのデータ修正だけで差し替えられる（コード変更不要）

### ST-M2-004-TC-001
- **観点ID**：ST-M2-004 ／ **対応要件**：AC-015 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. エンディングに1種以上到達していること（一覧の入口が出ている）

**手順**
1. 選択画面からエンディング一覧を開き、到達済みのエンディングを再生する

**確認**
1. 到達感 → 期待値：区切りとして成立している（演出・本文）
2. 記述の妥当性 → 期待値：**問題への解答で示せる範囲を超えた断定が無い**（「実務でできる」と言い切っていない・PO指摘 2026-09-06）

**後処理**
なし

**合否判定**
- PASS：両方成立／FAIL：未取得の実務能力を断定している記述がある
- ※ 本文は `public/data/endings.json` の修正だけで差し替えられる

### ST-M2-005-TC-001
- **観点ID**：ST-M2-005 ／ **対応要件**：AC-016 ／ **テストデータ**：なし ／ **担当**：人間

**事前条件**
1. 一度クリア済みのシナリオがあること（既読が記録されている）

**手順**
1. クリア済みシナリオを開き、⏩（既読スキップ）をONにする

**確認**
1. 速度 → 期待値：何が流れたか目で追える（現行 1ノードあたり約0.1秒）
2. 停止 → 期待値：選択肢で止まり、出題は飛ばされない

**後処理**
なし

**合否判定**
- PASS：両方成立／FAIL：速すぎて内容を追えない、または選択肢を飛ばす
- ※ 速度は定数1つで調整できる

---
